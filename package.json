{
  "name": "@darktalk/compiler",
  "version": "0.1.0",
  "private": true,
  "description": "DarkTalk compiler foundation — talk to your application.",
  "type": "commonjs",
  "bin": {
    "dark": "bin/dark.js"
  },
  "scripts": {
    "build": "node bin/dark.js build examples/hello.app.talk",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
